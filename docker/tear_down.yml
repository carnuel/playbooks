- hosts: "{{ hosts }}"
  vars:
    hosts: nodes
  sudo: yes
  gather_facts: yes
  remote_user: dbnet
  tasks:

  - name: Removing resources
    shell: rm -rf /home/dbnet/resources

  - name: clear containers
    shell: clearContainers
