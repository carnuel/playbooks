- hosts: cluster
  sudo: yes
  remote_user: dbnet
  tasks:

  - name: Install experimental build
    register: installed
    shell: curl -sSL https://experimental.docker.com/ | sh

  - name: Add user to group docker
    when: installed|success
    shell: adduser dbnet docker

